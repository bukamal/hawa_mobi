# مراجعة تقنية وأمنية لمشروع Android — Phase 98

## نطاق المراجعة

تمت مراجعة مشروع Flet/Python الخاص بتطبيق Android من حيث بنية المشروع، تسجيل الدخول، تخزين كلمات المرور والرموز، الاتصال بخادم Windows، الصلاحيات، النسخ الاحتياطي، SQL، والاختبارات والبناء.

## النتيجة العامة

البنية الوظيفية للمشروع واسعة ومغطاة بعدد جيد من اختبارات Smoke/Contract. التعديل الجديد لحفظ كلمة المرور اجتاز جميع الاختبارات ضمن بيئة مطابقة للاعتمادات المثبتة. لا توجد مشكلة تمنع اعتماد التعديل من ناحية كود Python. بقي اختبار APK الفعلي على جهاز Android ضرورياً لأن بيئة المراجعة لم تستطع تنزيل Flutter 3.29.2 بسبب تعطل DNS الخارجي.

## ما تم إصلاحه في هذه المرحلة

1. حفظ اسم المستخدم وكلمة المرور بعد نجاح تسجيل الدخول فقط.
2. تشفير كلمة المرور وعدم وضعها في SQLite/settings.
3. فصل بيانات الدخول بحسب الوضع المحلي أو عنوان الخادم.
4. إزالة بيانات الدخول عند تغيير الحساب أو عند ثبوت أنها قديمة.
5. تعطيل Android Auto Backup.
6. تثبيت إصدار `flet-permission-handler==0.1.0` لمنع سحب إصدار حديث يفرض ترقية Flet قسراً.
7. إضافة اختبارات وظيفية وأمنية إلى Quality Gate.

## نتائج الاختبار

- `compileall`: ناجح.
- اختبارات Quality Gate والاختبار الإضافي لواجهة الدخول: **69/69 ناجحة**.
- اختبار المخزن المشفر: ناجح.
- اختبار دورة الحفظ، الاسترجاع، فشل كلمة المرور القديمة، والمسح: ناجح.
- APK release preflight: ناجح.
- Bandit على كود التطبيق، مع استبعاد أدوات الاختبار والخادم:
  - High: صفر.
  - Medium: 8 تنبيهات.
  - Low: 254 تنبيهاً، أغلبها `try/except/pass` أو نصوص ترجمة اعتبرها الفاحص كلمات مرور ثابتة.
- بناء APK: بدأ بنجاح، ثم توقف قبل مرحلة البناء بسبب عدم قدرة البيئة على تنزيل Flutter 3.29.2 من الإنترنت. لم يكن التوقف ناتجاً عن خطأ في المشروع.

## المخاطر المفتوحة

### 1. الاتصال بخادم Windows عبر HTTP — خطورة عالية

واجهة الإعدادات تشجع على عناوين مثل `http://192.168.1.100:8000`، و`RestClient.login()` يرسل اسم المستخدم وكلمة المرور كـ JSON عبر هذا الاتصال. على شبكة غير موثوقة يمكن اعتراض كلمة المرور والرمز. المطلوب إنتاجياً: HTTPS بشهادة صحيحة، أو VPN موثوق، أو قناة TLS داخل الخادم.

### 2. رمز جلسة الشبكة محفوظ كنص صريح — خطورة عالية

`auth/network_token` يُكتب حالياً داخل جدول `settings` المحلي. الرمز قابل لإعادة الاستخدام إلى أن تنتهي صلاحيته. يجب نقله في مرحلة لاحقة إلى `CredentialStore` أو Android Keystore، وحذفه من النسخ الاحتياطية.

### 3. النسخ الاحتياطية اليدوية قد تتضمن رمز الجلسة — خطورة عالية

تعطيل `allowBackup` يمنع النسخ الاحتياطي التلقائي لنظام Android، لكنه لا يمنع خدمة النسخ الاحتياطي داخل التطبيق من نسخ قاعدة SQLite. طالما الرمز موجود داخل `settings` فقد يدخل في ملف النسخة الاحتياطية.

### 4. صلاحيات تخزين واسعة — خطورة متوسطة إلى عالية

المشروع يطلب `MANAGE_EXTERNAL_STORAGE` وقراءة/كتابة التخزين ووسائط متعددة. هذه الصلاحيات أوسع من المطلوب لتطبيق محاسبي وتزيد أثر أي ثغرة، كما قد تسبب مشاكل في مراجعة Google Play. يوصى بالاعتماد على FilePicker/Storage Access Framework ومساحة التطبيق الخاصة، ثم إزالة الصلاحيات تدريجياً بعد اختبار الاستيراد والتصدير.

### 5. قفل محاولات الدخول محلي ومؤقت — خطورة متوسطة

الحد الحالي خمس محاولات وقفل 60 ثانية موجود داخل كائن واجهة الدخول فقط؛ إعادة تشغيل التطبيق تعيد العداد. يجب فرض Rate Limiting في طبقة المصادقة نفسها، وبالأخص على خادم Windows، مع سجل تدقيق للمحاولات الفاشلة.

### 6. خوارزمية كلمة المرور المحلية قديمة نسبياً — خطورة متوسطة

التخزين المحلي يستخدم PBKDF2-SHA256 بعدد 100,000 دورة، ولا يخزن رقم إصدار الخوارزمية أو عدد الدورات، والمقارنة ليست عبر `hmac.compare_digest`. يوصى بمخطط إصدار يدعم ترقية تدريجية إلى Argon2id أو PBKDF2 بعدد أعلى مع مقارنة ثابتة الزمن.

### 7. بناء SQL بأسماء جداول نصية — خطورة منخفضة حالياً/متوسطة تصميمياً

Bandit أشار إلى استعلامات من نوع `SELECT * FROM {table}`. الاستخدام الحالي يمرر قوائم ثابتة، لذلك لا توجد قابلية استغلال مباشرة ظاهرة. يجب رغم ذلك وضع Allowlist داخل `FileExportService.create_csv_archive()` وعدم قبول أي اسم جدول خارجها.

### 8. ابتلاع الاستثناءات — خطورة تشغيلية متوسطة

يوجد عدد كبير من `except Exception: pass`. هذا يمنع انهيار الواجهة أحياناً، لكنه يخفي أعطال التخزين والاتصال ويصعب التحقيق. الأفضل تسجيل أخطاء مختصرة غير حساسة مع رمز حدث، وعدم إظهار كلمات المرور أو Tokens في السجل.

## توصية الاعتماد

يمكن اعتماد تعديل «حفظ كلمة المرور» بعد اختبار APK على جهاز Android فعلي بالسيناريوهات التالية:

1. حساب محلي صحيح ثم إغلاق التطبيق وفتحه.
2. حساب Windows Server صحيح ثم إغلاق التطبيق وفتحه.
3. تغيير كلمة المرور من جهاز آخر ثم فتح التطبيق والتحقق من حذف الكلمة القديمة بعد الفشل.
4. تغيير عنوان الخادم والتحقق من عدم تعبئة كلمة مرور الخادم السابق.
5. مسح بيانات الدخول المحفوظة والتحقق من اختفاء ملف vault.
6. أخذ نسخة احتياطية واستعادتها والتأكد أن كلمة المرور المحفوظة لا تنتقل مع قاعدة البيانات.

الأولوية الأمنية التالية هي: **TLS للاتصال، ثم تشفير رمز الجلسة، ثم تقليل صلاحيات التخزين**.
