# المرحلة 109.1 — إصلاح زر تسجيل دفعة في شاشة متابعة الدفعات

## العطل

صف شاشة متابعة الدفعات مصدره جدول `payment_reminders`. لذلك يحتوي على معرفين:

- `id`: معرف التذكير.
- `expense_id`: معرف القيد المالي القابل للسداد.

كانت نافذة `PaymentDialog` تعتمد دائمًا على `record["id"]`. عند اختلاف معرف التذكير عن معرف القيد كانت تحاول تحميل ملخص قيد آخر أو قيد غير موجود، فيفشل إنشاء النافذة داخل callback ويبدو زر «تسجيل دفعة» وكأنه لا يستجيب.

## الإصلاح

أضيفت خدمة `services/payment_target_service.py` لتوحيد تحديد القيد المستهدف وفق الترتيب:

1. `expense_id` لصفوف التذكيرات.
2. `target_expense_id` لصفوف الدفعات.
3. `id` لصفوف القيود العادية.

وتقوم الخدمة بما يلي:

- تثبيت `id` و`expense_id` على معرف القيد المالي الحقيقي.
- حفظ معرف التذكير الأصلي في `reminder_id`.
- رفض السجل الذي لا يحتوي معرفًا ماليًا صالحًا برسالة واضحة.

أصبحت `PaymentDialog` تستخدم السجل الموحّد، كما أصبحت شاشة متابعة الدفعات تمسك أي استثناء أثناء فتح النافذة وتعرض Snackbar بدل الفشل الصامت.

## الاختبارات

أضيف اختبار `payment_reminder_button_target_smoke_test.py` ينشئ عمدًا الحالة التالية:

- معرف التذكير: 1.
- معرف القيد الحقيقي: 4.

ثم يسجل دفعة 250 USD ويتحقق من:

- تسجيلها على القيد 4.
- بقاء القيد 1 دون دفعة.
- بقاء معرف التذكير محفوظًا.

وأضيف اختبار UI باسم `payment_reminders_button_ui_smoke_test.py` ليتحقق في بيئة Flet من:

- تمرير معرف القيد الصحيح إلى النافذة.
- استدعاء `open_control`.
- إظهار رسالة خطأ عند فشل بناء النافذة.

## الأثر

- لا يوجد تغيير في مخطط قاعدة البيانات.
- لا يوجد ترحيل بيانات.
- لا يتغير منطق الدفعات الجزئية أو المجمعة.
- يعمل الإصلاح في الوضع المحلي ووضع عميل الشبكة لأن كلاهما يعيد `expense_id` في صف التذكير.
