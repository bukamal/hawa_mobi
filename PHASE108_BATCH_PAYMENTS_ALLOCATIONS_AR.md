# المرحلة 108 — الدفعات المجمعة وتوزيعها على عدة مطالبات

## الهدف

تمكين المستخدم من تسجيل حركة قبض أو دفع واحدة للطرف، ثم توزيعها على أكثر من قيد أو خدمة، بدل إدخال دفعة منفصلة لكل مطالبة.

تدعم المرحلة:

- القيود العادية.
- الخدمات المباشرة.
- ملفات الخدمة متعددة المكونات.
- الشركات العميلة.
- الشركات الموردة.
- المسافرين المرتبطين بالشركات.
- القبض من العملاء والمسافرين.
- الدفع للموردين.
- المبالغ الزائدة كرصيد دائن أو دفعة مقدمة.

## طريقة الاستخدام

توجد نقطة دخول في:

1. شاشة **متابعة الدفعات** عبر زر «دفعة مجمعة».
2. شاشة **تفاصيل الشركة** عبر زر «دفعة مجمعة».
3. بطاقة مطالبة معلقة عبر زر «توزيع دفعة».

يختار المستخدم:

- الشركة كلها أو مسافرًا محددًا داخل الشركة.
- قبض من العميل/المسافر أو دفع للمورد.
- العملة.
- مبلغ الحركة.
- طريقة الدفع والتاريخ والمرجع.
- طريقة التوزيع:
  - تلقائيًا على أقدم المطالبات.
  - يدويًا على مطالبات مختارة.

## مثال

لدى شركة النور ثلاث مطالبات بالدولار:

- قيد عادي متبقٍ: 400 USD.
- خدمة مباشرة متبقية: 800 USD.
- ملف خدمة متبقٍ: 1,500 USD.

عند تسجيل دفعة مجمعة بقيمة 1,000 USD بطريقة «الأقدم أولًا»:

- 400 USD للقيد العادي.
- 600 USD للخدمة المباشرة.
- يبقى على الخدمة المباشرة 200 USD.
- يبقى ملف الخدمة 1,500 USD.

عند تسجيل 500 USD بالتوزيع اليدوي:

- 200 USD للخدمة المباشرة.
- 200 USD لملف الخدمة.
- 100 USD غير موزعة تتحول إلى رصيد دائن للطرف.

## الرصيد الزائد

إذا كانت الحركة قبضًا من عميل أو مسافر، يتحول المبلغ الزائد إلى:

- **رصيد دائن للعميل** — قيد «له» على المكتب.

إذا كانت الحركة دفعًا لمورد، يتحول المبلغ الزائد إلى:

- **دفعة مقدمة للمورد** — قيد «لنا» على المورد.

الرصيد الناتج يظهر في كشف حساب الشركة، وهو قابل للسداد أو الاسترداد لاحقًا عبر نظام الدفعات المعتاد.

## قاعدة البيانات

أضيفت الجداول:

- `payment_batches`: رأس الحركة النقدية الواحدة.
- `payment_allocations`: توزيع الحركة على المطالبات.

وأضيف العمود:

- `payments.batch_id`: ربط كل دفعة موزعة برأس الدفعة المجمعة.

تم رفع `schema_version` من 24 إلى 25، مع ترحيل تلقائي لقواعد البيانات السابقة دون حذف البيانات.

## ضوابط المحاسبة

- لا تجمع مطالبات بعملات مختلفة داخل دفعة واحدة.
- لا تجمع القبض من العملاء مع الدفع للموردين داخل الحركة نفسها.
- لا يجوز أن يتجاوز التوزيع اليدوي مبلغ الدفعة.
- لا يجوز أن يتجاوز تخصيص مطالبة مقدارها المتبقي.
- لا يمكن حذف جزء واحد من دفعة مجمعة؛ يجب حذف رأس الدفعة كاملًا.
- إذا استُخدم الرصيد الدائن أو رُد جزء منه، يُمنع حذف الدفعة الأصلية.
- عند حذف خدمة أخذت جزءًا من دفعة مجمعة، لا تُحذف الحوالة ولا توزيعات الخدمات الأخرى؛ يعود الجزء المحذوف إلى رصيد الطرف.
- جميع العمليات تتم داخل SQLite Transaction واحدة مع `ROLLBACK` عند الخطأ.

## الحذف

من شاشة سجل الدفعات المجمعة يمكن للمحاسب أو المدير حذف الدفعة كاملة مع سبب إلزامي.

عند الحذف:

- تحذف توزيعاتها.
- تحذف قيود التسوية التابعة لها.
- تعاد المطالبات إلى أرصدتها السابقة.
- يحذف الرصيد الزائد إذا لم يكن قد استُخدم.
- تحفظ العملية في `audit_log`.

## وضع الشبكة

أضيفت واجهات REST:

- `GET /api/payment-batches/outstanding`
- `GET /api/payment-batches`
- `GET /api/payment-batches/{id}`
- `POST /api/payment-batches`
- `DELETE /api/payment-batches/{id}`

وأضيفت قدرة الخادم:

- `supports_batch_payments = true`
- `batch_payments_contract = batch-payments-v1`

تطبيق Android يرفض الربط بخادم قديم لا يعلن دعم الدفعات المجمعة.

## الاختبارات

نجحت الاختبارات التالية:

- `compileall` لجميع ملفات Python.
- عقد المرحلة 108.
- ترحيل قاعدة بيانات قديمة من schema 24 إلى 25.
- التوزيع التلقائي على الأقدم.
- التوزيع اليدوي على عدة مطالبات.
- الرصيد الدائن للعميل.
- الدفعة المقدمة للمورد.
- منع حذف تخصيص منفرد من دفعة مجمعة.
- منع حذف دفعة أصلية بعد استخدام رصيدها.
- إعادة تخصيص مبلغ الخدمة المحذوفة إلى الرصيد الدائن.
- حذف الدفعة المجمعة وإعادة الأرصدة.
- انحدار الدفعات الجزئية للمرحلة 107.
- حذف قيود الشركات.
- ملفات الخدمة.
- الخدمات المباشرة.
- CRUD المحلي.
- عقد الربط اليدوي وقدرات الخادم.

بوابة الجودة اجتازت الاختبارات غير المعتمدة على واجهة Flet، ثم توقفت عند `phase100_design_system_smoke_test.py` لأن حزمة `flet` غير مثبتة في بيئة الفحص.

اختبار REST الحي لم يُشغّل لأن حزمة `flask` غير مثبتة؛ نجح عقد REST الساكن، وملف اختبار REST الحي مضمّن للتشغيل في بيئة الخادم.

## حدود المرحلة

- لا يوجد زر مخصص حتى الآن لتسوية رصيد دائن مع مطالبة جديدة دون حركة نقدية؛ يمكن رد الرصيد أو دفعه عبر الدفعات، أما المقاصة الداخلية الآلية فتحتاج كيان `credit_applications` مستقل.
- لم يُبنَ APK ولم تُختبر الشاشة على جهاز Android فعلي بسبب غياب Flet Runtime.
