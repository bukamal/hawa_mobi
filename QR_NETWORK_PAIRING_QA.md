# QR Network Pairing QA

## على Windows Server

1. شغّل الخادم من النسخة الحالية.
2. تأكد أن الخادم يعمل على IP الشبكة وليس `localhost` فقط.
3. سجّل الدخول كمستخدم `admin` أو `manager`.
4. من شاشة الربط في Windows، أنشئ QR بإرسال `server_url` مثل:

```text
http://192.168.1.50:8000
```

5. يجب أن يكون QR صالحاً لمدة 5 دقائق فقط.

## على Android

1. افتح التطبيق.
2. من شاشة تسجيل الدخول اضغط: `ربط مع Windows عبر QR`.
3. امسح QR أو الصق نص QR.
4. يجب أن تظهر رسالة نجاح.
5. بعد الربط، سجّل الدخول بحساب الخادم.

## فحوصات إلزامية

- لا تستخدم `localhost` أو `127.0.0.1` داخل Android.
- إذا انتهت صلاحية QR يجب أن يرفضه التطبيق.
- إذا كان الخادم لا يدعم `historic-currency-snapshot-v1` يجب أن يرفضه Android.
- بعد الربط لا يجب أن يدخل المستخدم تلقائياً. تسجيل الدخول يبقى منفصلاً.
- بعد تغيير IP جهاز Windows، يجب إعادة الربط عبر QR.

## سيناريو مالي بعد الربط

1. من Android أضف قيداً بعملة SYP.
2. من Windows غيّر سعر SYP.
3. من Android عدّل نفس القيد بدون تغيير العملة.
4. يجب أن يبقى السعر التاريخي القديم محفوظاً.
