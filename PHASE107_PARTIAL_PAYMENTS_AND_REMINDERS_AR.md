# المرحلة 107 — الدفعات الجزئية وتذكيرات السداد

## الهدف

إضافة نموذج محاسبي يفصل بين أصل المطالبة المالية وبين الدفعات الفعلية، بحيث يمكن تسجيل إجمالي الخدمة أو القيد، ثم قبض أو دفع المبلغ على دفعات، مع حساب المدفوع والمتبقي والحالة تلقائيًا.

## السلوك الجديد

- يبقى القيد الأصلي ممثلًا للإجمالي المستحق.
- كل دفعة تحفظ كسجل مستقل في جدول `payments`.
- ينشئ النظام حركة تسوية مقابلة في كشف حساب الطرف، ولا يعدّل مبلغ القيد الأصلي.
- الحالات محسوبة آليًا: غير مدفوع، مدفوع جزئيًا، مدفوع بالكامل.
- لا يمكن خفض إجمالي المطالبة إلى أقل من مجموع الدفعات المسجلة.
- لا يمكن تغيير عملة مطالبة عليها دفعات.
- عند اكتمال السداد يغلق تذكير الدفع تلقائيًا.
- مبلغ التذكير المعروض يحسب من المتبقي الحالي، وليس من مبلغ قديم ثابت.

## المسارات المدعومة

### القيد العادي القابل للسداد

يمكن إدخال:

- الإجمالي.
- الدفعة الأولى.
- طريقة الدفع.
- تاريخ الاستحقاق.
- مرجع الدفع وملاحظاته.

ويعرض كشف الشركة الإجمالي والمدفوع والمتبقي مع إمكانية تسجيل دفعات لاحقة وعرض سجلها.

### الخدمة المباشرة

يعمل مساران مستقلان:

- المبلغ المطلوب من العميل أو المسافر، والدفعات المقبوضة منه.
- تكلفة المورد، والدفعات المدفوعة للمورد.

لا يخلط النظام بين قبض العميل ودفع المورد.

### ملف الخدمة

- يسجل إجمالي البيع على العميل أو المسافر.
- يمكن تسجيل دفعة أولى ودفعات لاحقة.
- يحسب المدفوع والمتبقي وحالة السداد.
- تبقى تكلفة كل مورد منفصلة عن دفعات العميل.

## واجهة متابعة الدفعات

أضيفت شاشة `متابعة الدفعات` وتعرض:

- الطرف والمسافر عند وجوده.
- الإجمالي.
- المدفوع.
- المتبقي.
- تاريخ الاستحقاق وحالة التأخير.
- إجراء تسجيل دفعة أو فتح الشركة.

كما أضيف Dialog لتسجيل دفعة، عرض سجل الدفعات، وحذف دفعة بصلاحية وسبب.

## الحذف

- حذف العملية الأصلية يحذف دفعاتها وحركات التسوية التابعة داخل Transaction واحدة.
- لا تنشأ قيود عكسية جديدة في مسار الحذف.
- حذف دفعة منفردة يتطلب سببًا، ويزيل حركة التسوية التابعة لها ثم يعيد حساب الحالة والتذكير.
- يستمر تسجيل الحدث في سجل التدقيق.

## ترحيل قاعدة البيانات

رُفع إصدار المخطط إلى 24، وأضيف:

- `expenses.is_settleable`
- `expenses.payment_status`
- جدول `payments` وفهارسه

تُعامل قيود التسوية والعكس القديمة كقيود غير قابلة للسداد لتجنب إنشاء دفعات فوق دفعات.

## REST ووضع الشبكة

أضيفت واجهات:

- `GET /api/expenses/<id>/payment-summary`
- `GET /api/expenses/<id>/payments`
- `POST /api/expenses/<id>/payments`
- `DELETE /api/payments/<id>`

كما أضيفت capabilities خاصة بالدفعات الجزئية مع الحفاظ على رقم عقد API القديم للتوافق.

## حدود هذه المرحلة

هذه المرحلة لا تنفذ بعد:

- دفعة واحدة عامة موزعة على عدة خدمات عبر `payment_allocations`.
- رصيد دائن أو دفعة زائدة تتجاوز إجمالي المطالبة.
- تحصيل جماعي لشركة عميلة ثم توزيعه تلقائيًا على أقدم المطالبات.
- إشعارات Android المحلية أو إرسال WhatsApp تلقائي؛ الشاشة والتذكيرات داخل التطبيق مدعومة فقط.

يمنع الإدخال الحالي الدفعة الأكبر من المتبقي لتجنب رصيد دائن غير ممثل محاسبيًا.

## التحقق

نجحت اختبارات:

- ترجمة جميع ملفات Python.
- عقد المرحلة 107.
- التكامل المحاسبي للدفعات الجزئية.
- القيد العادي: 1000 إجمالي، 400 مدفوع، 600 متبقي، ثم سداد كامل.
- الخدمة المباشرة: العميل 1000/400/600 والمورد 700/300/400.
- ملف الخدمة: 3000/1200/1800.
- إغلاق التذكير عند اكتمال السداد.
- منع خفض الإجمالي تحت المدفوع.
- حذف العمليات ودفعاتها دون سجلات يتيمة.
- CRUD المحلي وحذف جدول قيود الشركة.
- اختبارات الخدمات والتقارير وتهيئة APK السابقة ضمن بوابة الجودة.

توقفت بوابة الجودة عند اختبار تصميم يستورد `flet` لأن المكتبة غير مثبتة في بيئة الفحص. لم يتم بناء APK أو تشغيل واجهة Flet أو خادم Flask فعليًا في هذه البيئة.
