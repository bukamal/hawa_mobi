# تقرير اعتماد Android 1.1.1 — إصلاح CI وعقد API

## سبب الفشل

كان المسار `/api/service_cases` موجودًا في `database/connection_rest.py`، لكن الاختبار الساكن بحث عن النص بعلامات اقتباس مفردة فقط. بعد تنسيق Ruff/Black صار الكود يستخدم علامات مزدوجة، فظهر خطأ زائف عند غياب Flask في GitHub Actions.

## التصحيح

- استبدال فحص النصوص الهش بتحليل AST لطرق `RestClient` وثوابت المسارات.
- تثبيت `server/requirements.txt` في GitHub Actions لتنفيذ اختبار Flask الحي.
- تحسين مشغّل `quality_gate.py` بعدم استخدام stdout/stderr pipes التي قد تبقيها عمليات Flet الفرعية مفتوحة.
- رفع الإصدار إلى 1.1.1.

## قاعدة البيانات القديمة

لا يوجد أي تعديل على مخطط قاعدة البيانات أو ترحيلات SQLite أو صيغة النسخ الاحتياطية مقارنة بالإصدار 1.1.0. مسار استيراد القواعد القديمة، فحص السلامة، النسخة الاحتياطية السابقة للاستيراد، والترجيع التلقائي بقيت كما هي.

## نتائج التحقق

- الفحص الساكن عند غياب Flask: ناجح.
- اختبار Flask الحي: ناجح.
- جميع سكربتات بوابة الجودة الخمسين: ناجحة في مجلدات بيانات معزولة.
- Ruff: صفر مخالفات.
- Bandit: صفر نتائج عالية، صفر متوسطة، 179 منخفضة.
- `compileall`: ناجح.
- `pip check` داخل بيئة افتراضية نظيفة: ناجح.
- بناء Wheel وsdist للإصدار 1.1.1: ناجح.
- APK preflight: ناجح.

ملاحظة: لم يتم تجميع APK محليًا في هذه الجلسة؛ ملف GitHub Actions مصحح ليكمل البناء بعد نجاح بوابة الجودة.
