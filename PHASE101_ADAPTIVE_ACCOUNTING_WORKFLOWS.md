# Phase 101 — Adaptive Accounting Workflows

الإصدار: **1.0.45**

## الهدف

استكمال نظام التصميم الموحد بتحويل أكثر النماذج المحاسبية ازدحامًا إلى تدفقات متدرجة قابلة للاستخدام على الهواتف الصغيرة والتابلت، دون تغيير قاعدة البيانات أو العقود المحاسبية.

## ما تم تنفيذه

### ملف الخدمة متعدد الموردين

تحول من نموذج طويل واحد إلى أربع خطوات:

1. بيانات العميل والمسافر.
2. المورد والخدمة الأساسية.
3. رسوم السفارة والنقل الاختيارية.
4. مراجعة الأثر المالي والتاريخ والملاحظات والحفظ.

تظهر في المراجعة:

- إجمالي البيع على العميل.
- إجمالي المستحق للموردين.
- الربح الداخلي.
- جميع أطراف وبنود العملية.

### الخدمة المباشرة

تحولت إلى ثلاث خطوات:

1. الحساب والمسافر ونوع الخدمة.
2. البيع والتكلفة والمورد.
3. مراجعة الأثر المالي والتاريخ والملاحظات والحفظ.

يدعم التدفق وضع المورد فقط، والتعديل مع سبب إلزامي، والتحذير عند تجاوز التكلفة للبيع.

### مكونات التصميم الجديدة

أضيف `views/design_system/workflow.py` ويتضمن:

- `WorkflowStep`
- `WorkflowController`
- `adaptive_dialog_metrics()`
- `section_card()`
- `financial_summary()`
- `review_row()`

## الاستجابة للمقاييس

تم اختبار إنشاء وتنقل التدفقات على:

- 320×568
- 360×800
- 412×915
- 800×1100

تستخدم الأزرار التفافًا تلقائيًا على الشاشات الضيقة، ويكون للخطوات Scroll رئيسي واحد.

## المحافظة على المنطق

لم تتغير:

- بنية Payload للخدمات.
- قواعد التحقق في طبقة الخدمات.
- المستودعات المحلية أو الشبكية.
- القيود المرتبطة.
- حساب البيع والتكلفة والربح.
- منطق العكس والإخفاء التشغيلي.
- حفظ كلمة المرور المشفرة.

## تحسين إضافي

تم إصلاح محلل المبالغ المشترك ليقبل الحقول الفارغة كصفر في البنود الاختيارية، مع دعم الفاصلة العربية والعشرية، بدل إظهار خطأ «المبلغ غير صالح» عند ترك بند اختياري فارغًا.

## الاختبارات

- `compileall`: ناجح.
- Quality Gate الكامل: ناجح.
- اختبار Phase 101 للتدفقات المتدرجة: ناجح.
- اختبارات الخدمات المباشرة وملفات الخدمة والعكس والتقارير: ناجحة.
