# Phase 98 — Secure Remembered Login Password

## الهدف

إضافة خيار حفظ اسم المستخدم وكلمة المرور في واجهة تسجيل الدخول دون وضع كلمة المرور كنص صريح داخل SQLite أو ملفات الإعدادات.

## التنفيذ

- أضيف `auth/credential_store.py` كمخزن بيانات دخول مشفّر.
- تُشفّر البيانات بواسطة `cryptography.fernet.Fernet`.
- تُحفظ الملفات داخل مساحة بيانات التطبيق الخاصة في مجلد `secure/`:
  - `login_credentials.key`
  - `login_credentials.vault`
- صلاحيات الملفات على الأنظمة الداعمة هي `0600`، وصلاحية المجلد `0700`.
- يُربط الحفظ بنطاق الدخول:
  - `local` للحساب المحلي.
  - عنوان Windows Server المحدد لوضع عميل الشبكة.
  هذا يمنع تعبئة كلمة مرور خادم في حساب محلي أو خادم آخر.
- لا تُحفظ كلمة المرور إلا بعد نجاح التحقق منها.
- عند تغيير الحساب تُحذف بيانات الدخول المحفوظة.
- إذا فشلت بيانات الدخول التي تمت تعبئتها تلقائياً، تُحذف لمنع تكرار محاولات خاطئة عند كل تشغيل.
- لا يوجد دخول تلقائي؛ يتم تعبئة الحقول ويظل ضغط زر «تسجيل الدخول» مطلوباً.
- تم تعطيل Android Auto Backup عبر `allowBackup = "false"`.
- تم تثبيت الاعتمادات المتوافقة:
  - `flet==0.28.3`
  - `flet-permission-handler==0.1.0`
  - `cryptography==43.0.1`

## الاختبارات المضافة

```bash
PYTHONPATH=. python tools/credential_store_smoke_test.py
PYTHONPATH=. python tools/login_remember_password_flow_test.py
PYTHONPATH=. python tools/ui_auth_smoke_test.py
PYTHONPATH=. python tools/apk_release_preflight.py
```

تغطي الاختبارات:

- الحفظ والاسترجاع المشفّر.
- عدم ظهور اسم المستخدم أو كلمة المرور كنص واضح في ملفات المخزن.
- فصل نطاق الحساب المحلي عن خوادم الشبكة.
- اكتشاف العبث بالملف المشفّر دون انهيار التطبيق.
- تعبئة الحقول عند إعادة فتح شاشة الدخول.
- حذف كلمة المرور القديمة بعد فشل التحقق.
- حذف البيانات عند اختيار «مسح بيانات الدخول المحفوظة».
- منع تخزين كلمة المرور داخل جدول `settings`.

## حد الأمان

هذا الحل مناسب لإصدار Flet الحالي ويحمي من التسريب العرضي، والنسخ الاحتياطي للنظام، وقراءة SQLite أو الملفات العادية. مفتاح التشفير موجود داخل مساحة التطبيق الخاصة؛ لذلك لا يقدم حماية كاملة إذا كان الجهاز Root أو تم اختراق عملية التطبيق بالكامل. عند ترقية Flet إلى إصدار حديث، يوصى بنقل الخلفية إلى `flet-secure-storage`/Android Keystore مع الإبقاء على واجهة `CredentialStore` نفسها.
