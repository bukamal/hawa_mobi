# المرحلة 109 — إشعارات Android المحلية عند إغلاق التطبيق

## نطاق التنفيذ

تم تنفيذ المرحلة الأولى فقط، دون WhatsApp ودون Firebase/FCM. تعتمد المرحلة على جدولة Android الأصلية بحيث تبقى المواعيد محفوظة خارج دورة حياة واجهة Python/Flet وتظهر عندما يكون التطبيق في الخلفية أو مغلقًا إغلاقًا عاديًا.

## الوظائف المضافة

- إضافة Flet محلية متوافقة مع خط المشروع `Flet 0.28.3` ومبنية على `flutter_local_notifications`.
- طلب إذن `POST_NOTIFICATIONS` على Android 13 وما بعده.
- إعادة جدولة التنبيهات بعد إعادة تشغيل الهاتف عبر `RECEIVE_BOOT_COMPLETED` ومستقبلات الإضافة الأصلية.
- استخدام `inexactAllowWhileIdle` بدل صلاحية المنبهات الدقيقة؛ تذكيرات الدفع لا تحتاج دقة بالثانية.
- قنوات منفصلة:
  - `hawaa_due_soon`: استحقاقات قريبة، أهمية عادية.
  - `hawaa_due_today`: استحقاقات اليوم، أهمية مرتفعة.
  - `hawaa_overdue`: مبالغ متأخرة، أهمية مرتفعة.
  - `hawaa_system`: إشعارات اختبار وحالة النظام.
- تسلسل افتراضي لكل مطالبة:
  - قبل الاستحقاق بـ3 أيام.
  - يوم الاستحقاق.
  - بعد التأخير بـ3 أيام.
  - بعد التأخير بـ7 أيام.
- عند تثبيت الإصدار وفيه مطالبة متأخرة قديمة، يظهر أحدث مستوى متأخر فقط؛ لا يرسل أربعة تنبيهات دفعة واحدة.
- أزرار داخل الإشعار:
  - فتح المطالبة.
  - تسجيل دفعة.
- الضغط على الإشعار يفتح شاشة متابعة الدفعات ويضع المطالبة المرتبطة أول القائمة. زر تسجيل دفعة يفتح نافذة الدفع للمطالبة نفسها.
- إلغاء التنبيهات تلقائيًا عند اكتمال السداد أو حذف المطالبة أو تغيير تاريخ الاستحقاق.
- منع التكرار بمفتاح ثابت ومعرف Android ثابت لكل مطالبة ومرحلة استحقاق.
- خصوصية شاشة القفل بثلاثة أوضاع:
  - خاص: التفاصيل موجودة في الإشعار ويخفيها Android عند القفل.
  - عام: إظهار الاسم والمبلغ.
  - سري: نص عام دون اسم أو مبلغ.
- شاشة جديدة: **المزيد ← مركز التنبيهات**، وتشمل:
  - حالة الإضافة الأصلية والإذن.
  - تشغيل أو إيقاف التنبيهات.
  - وقت التنبيه اليومي.
  - عدد أيام التذكير المسبق.
  - خصوصية شاشة القفل.
  - اختبار إشعار فوري.
  - قائمة التنبيهات المجدولة وحالتها.

## قاعدة البيانات

رُفع `schema_version` من 25 إلى 26، وأضيف:

- `local_notification_schedule`: سجل جهاز محلي للتنبيهات المخططة والمجدولة والظاهرة والمفتوحة والفاشلة.
- `notification_state`: مؤشر تغيرات مالية لإعادة المزامنة.
- 9 Triggers على `expenses` و`payment_reminders` و`payments` لوسم الجدولة بأنها تحتاج تحديثًا بعد الإضافة أو التعديل أو الحذف.

إعدادات التنبيهات محفوظة في SQLite المحلية للجهاز حتى في وضع عميل الشبكة؛ لا تُرفع إلى الخادم ولا تتداخل إعدادات جهاز مع جهاز آخر.

## بناء Android

أضيفت حزمة محلية داخل:

`extensions/flet_notifications`

وتم تحديث GitHub Actions لتثبيتها قبل المشروع. بعد توليد مشروع Flutter يشغّل البناء:

`tools/patch_android_local_notifications.py`

وهو يضيف بصورة idempotent:

- مستقبل التنبيه المجدول.
- مستقبل إعادة التشغيل.
- مستقبل أزرار الإجراءات.
- Java Core Library Desugaring.
- MultiDex.

ثم يعيد بناء APK من مشروع Flutter المولّد.

## القيود المقصودة

- لا يوجد WhatsApp في هذه المرحلة.
- لا يوجد FCM أو إرسال Push من الخادم.
- في وضع الشبكة، التغييرات التي تحدث على جهاز آخر أثناء إغلاق هذا الهاتف لا يمكنها تعديل تنبيه الهاتف فورًا؛ تتم إعادة المطابقة عند فتح التطبيق، أو كل خمس دقائق أثناء بقائه مفتوحًا. المعالجة الفورية أثناء الإغلاق تحتاج FCM في مرحلة لاحقة.
- Android لا يضمن تنفيذ تنبيهات التطبيق بعد قيام المستخدم بـForce Stop من إعدادات النظام حتى يفتح التطبيق مرة أخرى.
- لم يُبنَ APK داخل بيئة الفحص الحالية لعدم توفر Flet/Flutter؛ يجب بناء APK عبر GitHub Actions واختباره على جهاز Android فعلي، بما في ذلك الإذن، إعادة التشغيل، السداد، الحذف، والضغط على الأزرار.

## اختبارات المرحلة

نجح `tools/phase109_local_notifications_smoke_test.py` في التحقق من:

- ترحيل المخطط إلى الإصدار 26.
- إنشاء الجداول والإعدادات والمشغلات.
- التخطيط الزمني والحماية من تنبيهات تاريخية متعددة.
- ثبات معرفات التنبيهات.
- عدم وجود WhatsApp في مسار المرحلة.
- إلغاء أربع إشعارات تلقائيًا بعد سداد المطالبة بالكامل.
- Patch Android مرتين دون تكرار عناصر Manifest أو Gradle.
- استخدام جدولة غير دقيقة وعدم طلب Exact Alarm.

كما نجحت اختبارات CRUD والدفعات الجزئية والدفعات المجمعة وحذف الخدمات وعقد الشبكة وتهيئة APK الساكنة.
