# دليل استيراد قاعدة بيانات قديمة — Android 1.1.0

## الصيغ المدعومة

- ملف قاعدة مباشر: `.db` أو `.sqlite` أو `.sqlite3`.
- ملف ZIP صادر من نسخة قديمة من التطبيق.
- ملف ZIP يحتوي قاعدة البيانات داخل مجلد فرعي.
- ملف ZIP متداخل يحتوي ZIP آخر بداخله، ضمن الحدود الأمنية المطبقة.

## آلية الحماية أثناء الاستيراد

1. يفحص التطبيق سلامة SQLite باستخدام `PRAGMA integrity_check`.
2. يتحقق من وجود جدول القيود `expenses` والأعمدة الأساسية: `company_name`, `amount`, `type`, `date`.
3. ينشئ نسخة أمان تلقائية من القاعدة الحالية.
4. ينسخ القاعدة القديمة إلى ملف مؤقت.
5. يرحّل النسخة المؤقتة إلى المخطط الحالي رقم 23.
6. يفحص النسخة المرحّلة مرة أخرى.
7. يستبدل القاعدة الحالية استبدالًا ذريًا فقط بعد نجاح جميع الخطوات.
8. إذا فشل التحقق النهائي، يعيد القاعدة وملف الإعدادات السابقين تلقائيًا.

## ما يتم الحفاظ عليه

- جميع القيود القديمة وقيم المبالغ والتواريخ والملاحظات والعملات.
- المستخدمون القدامى وكلمات مرورهم المتوافقة مع PBKDF2 القديم.
- عنوان خادم الشبكة الحالي المحفوظ على الجهاز، مع إجبار التطبيق على الوضع المحلي بعد الاستعادة.

## ما يتم حذفه أمنيًا

- `auth/network_token` القديم.
- وضع عميل الشبكة من النسخة المستوردة.
- السماح القديم باتصالات HTTP غير المشفرة.

## خطوات الاستخدام

1. احتفظ بالملف القديم الأصلي دون تعديل أو إعادة تسمية إلزامية.
2. ثبّت النسخة الجديدة دون حذف ملف النسخة القديمة.
3. افتح: الإعدادات ← النسخ الاحتياطي.
4. اختر «استيراد نسخة احتياطية خارجية» وحدد ملف ZIP أو DB.
5. إن لم يرجع منتقي Android الملف، استخدم «استيراد من Download/Hawaa» بعد وضع الملف في ذلك المجلد.
6. وافق على الاستعادة بعد مراجعة عدد المستخدمين والقيود الظاهر في شاشة الفحص.
7. بعد النجاح، تحقق من عدد القيود، أرصدة شركتين على الأقل، والعملات، ثم جرّب تسجيل الدخول بحساب قديم.
8. لا تحذف الملف القديم قبل إنهاء التحقق اليدوي.

## ملاحظة مهمة

تم اختبار الترحيل آليًا باستخدام قاعدة قديمة مماثلة لمخطط التطبيق السابق، بما في ذلك كلمة مرور PBKDF2 قديمة وبيانات متعددة العملات وملف إعدادات تالف. لا يمكن الجزم بسلامة ملفك المحدد قبل فحصه هو نفسه؛ ارفع نسخة منه عند الحاجة إلى تحقق مطابق للملف دون تعديل الأصل.
