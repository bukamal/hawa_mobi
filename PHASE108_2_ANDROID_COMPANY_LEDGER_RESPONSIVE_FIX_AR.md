# المرحلة 108.2 — إصلاح عرض قيود الشركة على Android

## العطل

كان مكوّن `DataTable` يُرسم داخل تمرير أفقي، بينما شاشة تفاصيل الشركة نفسها داخل تمرير عمودي. على شاشة الهاتف الضيقة كان Flutter قد يفشل في تخطيط الجدول العريض ويعرض مساحة رمادية كبيرة (`ErrorWidget`) بدل الصفوف.

## الإصلاح

- عرض أقل من 720: بطاقات محاسبية مضغوطة، بلا `DataTable` أو تمرير أفقي.
- عرض 720 فأكثر: الإبقاء على الجدول الكامل.
- كل بطاقة تعرض التاريخ، المصدر، البيان، المسافر/نوع الخدمة، اتجاه القيد، المبلغ، الرصيد الجاري، المدفوع، المتبقي، حالة السداد، وزر الإجراءات.
- إعادة بناء العرض تلقائيًا عند تدوير الجهاز إذا انتقل العرض بين النمط المضغوط والجدول.
- إزالة الحدود الدائرية من `DataTable` نفسه ووضعها على حاوية خارجية في العرض الواسع لتقليل أخطاء الرسم.
- تحديث اختبار المرحلة 103 ليتحقق من 20/25 بطاقة على الهاتف، ومن 20 صفًا داخل `DataTable` على الشاشة الواسعة.

## الملفات المعدلة

- `views/company_details_mobile_view.py`
- `views/app_layout.py`
- `tools/phase103_accounts_reports_performance_smoke_test.py`

## التحقق

نجح:

- `python -m compileall -q -f .`
- `tools/phase103_accounts_reports_performance_smoke_test.py` (الفحوص الساكنة في البيئة الحالية)
- `tools/company_ledger_table_hard_delete_smoke_test.py`
- `tools/phase104_navigation_recovery_accessibility_smoke_test.py`
- `tools/apk_release_preflight.py`
- اختبارات قاعدة البيانات والدفعات المجمعة حتى اختبار نظام التصميم ضمن بوابة الجودة.

لم يُنفذ اختبار Flet التشغيلي أو بناء APK في البيئة الحالية لعدم توفر `flet==0.28.3`. في بيئة GitHub Actions التي تحتوي Flet، اختبار المرحلة 103 يشغّل التحقق الفعلي للنمط المضغوط والجدول الواسع.
