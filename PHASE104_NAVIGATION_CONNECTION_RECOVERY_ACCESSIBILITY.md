# Phase 104 — Navigation, Connection Recovery and Accessibility

الإصدار: **1.0.48**

## الهدف

منع تعطل استخدام تطبيق Android عند تغيّر عنوان خادم Windows أو توقفه، وإضافة تنقل داخلي قابل للتتبع يدعم الرجوع بصورة منطقية، مع تحسين حالات التحميل والخطأ وأحجام اللمس دون تغيير المنطق المحاسبي أو قاعدة البيانات.

## ما تم تنفيذه

### استعادة الاتصال قبل تسجيل الدخول

- إضافة شاشة مستقلة تظهر عند تعذر الوصول إلى الخادم بدل شاشة خطأ مغلقة.
- تعديل عنوان الخادم واختباره وحفظه من داخل التطبيق.
- الربط عبر QR أو الرمز اليدوي من شاشة الاستعادة.
- عرض تفاصيل تقنية اختيارية بدل إغراق المستخدم بها افتراضيًا.
- تحذير واضح عند استخدام HTTP غير المشفر.
- السماح بالتحول إلى الوضع المحلي فقط بعد التحقق من اسم وكلمة مرور مدير موجود في قاعدة الجهاز المحلية.
- إبقاء كلمة المرور المحلية داخل الذاكرة المؤقتة للنموذج فقط وعدم حفظها.

### التنقل والرجوع

- إضافة مسارات ثابتة للشاشات الأساسية والإدارية.
- مسار مستقل لتفاصيل كل شركة مع ترميز الاسم داخل الرابط.
- العودة من تفاصيل الشركة إلى الحسابات.
- العودة من قسم إعدادات إلى مركز الإعدادات، ثم إلى «المزيد».
- دعم زر Escape ومسار `on_view_pop` وإتاحة `handle_back` مركزيًا.
- إغلاق الحوار المفتوح قبل تغيير الشاشة.
- تأكيد الخروج عند الرجوع من لوحة التحكم.
- الحفاظ على واجهات `switch_page()` و`open_company_details()` للتوافق مع الشاشات والاختبارات السابقة.

### أداء بدء التشغيل

- نقل فحص SQLite والخادم والترخيص والجلسة إلى `asyncio.to_thread()`.
- إزالة فترات الانتظار الاصطناعية الطويلة من Splash.
- نقل تجميع تنبيهات الدفع بعيدًا عن خيط الواجهة.

### حالات الواجهة والوصولية

- حالة تحميل موحدة مع Skeleton بسيط.
- حالة خطأ موحدة ورسالة إعادة محاولة.
- مكوّن Offline Banner جاهز للاستخدام.
- رفع أزرار الإجراءات المشتركة وأزرار القوائم إلى مساحة لمس 48dp.
- تحذير مرئي في شريط الحالة عند اتصال العميل عبر HTTP.
- إضافة مفردات عربية وإنجليزية وفرنسية لمسارات التنقل والاستعادة وحالات النظام.

## الضمانات

- لم يتغير مخطط قاعدة SQLite.
- لم تتغير عقود REST أو احتساب لنا/له أو الأرباح.
- لم تتغير سياسة القيود المعكوسة أو التقارير والطباعة.
- لم تتغير آلية حفظ كلمة المرور المشفرة.
- لا يسمح بالوضع المحلي من شاشة الاستعادة دون مصادقة مدير محلي.

## الاختبارات

- نجح `compileall`.
- نجح APK Release Preflight.
- نجحت جميع اختبارات Quality Gate وعددها 74 عند تشغيلها على دفعات.
- نجحت اختبارات UI والإدارة والمصادقة والشبكة الإضافية.
- نجح اختبار Phase 104 التشغيلي للمسارات، الرجوع، شاشة الاستعادة، والتحقق من المدير المحلي.
