# المرحلة 108.1 — إصلاح اختبار جدول قيود الشركة

## سبب الفشل

بعد تحويل قيود الشركة في المرحلة 106 من عناصر منفصلة إلى `DataTable`، بقي اختبار المرحلة 103 يعتمد على البنية القديمة:

```python
len(details.records_list.controls) == 20
```

القيمة أصبحت `1` لأن `records_list` يحتوي بطاقة جدول واحدة، بينما الصفوف المالية موجودة داخل `DataTable.rows`. لذلك كان الفشل في الاختبار، لا في ترقيم القيود أو بيانات الشركة.

## الإصلاح

أضيفت دالة اختبار تبحث داخل شجرة عناصر Flet عن الجدول الفعلي، ثم تعد صفوفه. أصبح الاختبار يتحقق من:

- 20 صفًا عند التحميل الأول.
- ظهور شريط الترقيم.
- 25 صفًا بعد الضغط على «عرض المزيد».
- استمرار فلاتر الاتجاه والشخص في العمل.

لم يتغير كود التطبيق أو قاعدة البيانات أو منطق الدفعات.

## التحقق المنفذ

- `python -m py_compile tools/phase103_accounts_reports_performance_smoke_test.py`: ناجح.
- `PYTHONPATH=. python tools/phase103_accounts_reports_performance_smoke_test.py`: ناجح في البيئة الحالية، مع تخطي الجزء التشغيلي تلقائيًا لعدم توفر Flet.
- تشغيل بوابة الجودة الكاملة وصل إلى `phase100_design_system_smoke_test.py` ثم توقف لعدم توفر حزمة `flet` في بيئة الفحص الحالية؛ لذلك يلزم إعادة تشغيلها في GitHub Actions حيث Flet مثبت للتحقق التشغيلي الكامل.
