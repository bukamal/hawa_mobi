# Phase 66 — Ledger Operation Core

هذه المرحلة تضيف أساسًا محاسبيًا منظمًا فوق جدول القيود القديم بدون كسر البيانات السابقة.

## ما أضيف

- حقول اختيارية داخل القيد:
  - `person_name`: اسم الزبون / المسافر داخل القيد.
  - `person_name_search`: نسخة مطبعة للبحث العربي.
  - `service_type`: نوع الخدمة مثل تذكرة سفر، فيزا، رسوم، تسديد.
  - `operation_type`: نوع العملية المحاسبية المنظم.
  - `is_locked`: منع تعديل/حذف القيود المولدة من عمليات مرتبطة.
  - `reversal_of` و `reversed_by`: تمهيد لعكس القيود بدل حذفها.

## التوافق

- القيود القديمة تبقى موجودة كما هي.
- القيود القديمة تأخذ:
  - `operation_type = normal`
  - `service_type = غير محدد`
  - `is_locked = 0`
- لا يتم تغيير المبالغ أو الأرصدة القديمة.
- البحث الشامل أصبح يشمل اسم الشخص ونوع الخدمة ونوع العملية.

## السلوك المحاسبي

أي قيد مرتبط بعملية مولدة مثل سداد بالنيابة يصبح مقفلًا ولا يُعدل أو يُحذف منفردًا. الإلغاء الصحيح لاحقًا يكون عبر عكس العملية وليس الحذف.

## التحقق

- `tools/ledger_operation_core_smoke_test.py`
- `compileall`
