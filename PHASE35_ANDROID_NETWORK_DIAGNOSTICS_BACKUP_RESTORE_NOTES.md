# Phase 35 — Android Network Diagnostics + Backup Restore

## الهدف
إصلاح نقطتين ظهرتا في الاختبار الميداني:

1. رسائل فشل الشبكة كانت تقنية وطويلة (`HTTPConnectionPool`, `Read timed out`, `Network is unreachable`) ولا تشرح للمستخدم ماذا يفعل.
2. شاشة النسخ الاحتياطي كانت تنشئ وتشارك نسخة، لكنها لا تملك مسار استيراد/استعادة آمن.

## الشبكة
أضيفت خدمة:

```text
services/network_diagnostics_service.py
```

وتحوّل أخطاء الشبكة إلى رسائل عربية مفهومة:

- الشبكة غير قابلة للوصول.
- انتهت مهلة الاتصال.
- الخادم رفض الاتصال.
- عنوان محلي غير مناسب للهاتف الحقيقي.

وتعرض خطوات فحص واضحة:

- فتح `/api/health` من متصفح الهاتف.
- التأكد أن Windows Server يعمل.
- التأكد من نفس شبكة Wi‑Fi.
- فتح المنفذ 8000 في الجدار الناري.
- ملاحظة خاصة لعناوين Hotspot مثل `192.168.43.x`.

## النسخ الاحتياطي والاستعادة
تمت إضافة استيراد نسخة احتياطية في Android Local mode فقط.

الاستيراد يدعم:

```text
.zip يحتوي hawaa_data.db
.db
.sqlite
.sqlite3
```

قبل الاستعادة يتم:

1. فحص الملف.
2. فحص `PRAGMA integrity_check`.
3. التأكد من وجود الجداول الأساسية.
4. عرض ملخص النسخة للمستخدم.
5. إنشاء safety backup من القاعدة الحالية.
6. إغلاق SQLite.
7. حذف ملفات WAL/SHM القديمة.
8. استبدال قاعدة البيانات.
9. تشغيل migrations/ensure_db.

في وضع العميل Client mode، يتم منع الاستيراد برسالة واضحة، لأن قاعدة البيانات الرئيسية تكون في Windows Server.

## ملاحظة مهمة
تم تعديل أسماء ملفات النسخ الاحتياطي لتتضمن microseconds حتى لا يحدث تصادم أسماء عند إنشاء safety backup في نفس ثانية اختيار ملف الاستعادة.

## الاختبارات
أضيفت:

```text
tools/backup_restore_smoke_test.py
tools/network_diagnostics_smoke_test.py
```

نتيجة الفحص:

```text
quality_gate passed
```
