# APK / Windows Compatibility Matrix

| المجال | الحالة بعد Phase 18 | ملاحظة |
|---|---|---|
| العملات والسعر التاريخي | متوافق منطقيًا | `amount_base` و Snapshot تم توحيدهما |
| قاعدة البيانات المحلية | متوافقة جزئيًا | APK يدعم Migration جديد، لكن لا ينصح بنقل DB يدويًا بين Android وWindows |
| REST API | أفضل من السابق | تم دعم `exchange_rate_history` وتنظيف Contract |
| الصلاحيات | محسنة | توجد حماية أدوار أولية، لكنها ليست بمستوى Windows الكامل بعد |
| الترخيص | محسّن | مسار دائم وصيغ تاريخ أوسع |
| APK packaging hygiene | محسّن | تم حذف ملفات server القديمة من الجذر وملفات الترخيص المضمنة |
| البناء الفعلي APK | يحتاج تجربة | يجب تشغيل GitHub Action أو `flet build apk` |

## النموذج الموصى به

Windows يعمل كخادم وقاعدة بيانات رئيسية. Android يعمل كعميل REST.

لا يوصى بمزامنة قواعد SQLite يدويًا بين الجهازين.
