name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk curl unzip zip git

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.7'
          channel: 'stable'

      - name: Accept Android licenses
        run: yes | flutter doctor --android-licenses || true

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install flet[build] requests bcrypt
          pip install -r requirements.txt || true

      - name: Debug project files
        run: |
          ls -la
          ls -la views/ || true
          cat main.py | head -20

      - name: Build APK with verbose output
        run: |
          flet build apk --yes --verbose --product "هوى الشام" --org com.hawaa

      - name: List build artifacts
        run: |
          find build -type f || true
          ls -la build/apk/release/ || true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hawaa-apk
          path: build/apk/release/*.apk
