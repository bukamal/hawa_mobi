# تقرير اعتماد إصدار Android 1.1.0

التاريخ: 2026-07-13

## النطاق

تم تعديل مشروع Android/Flet فقط. لا تحتوي الحزمة النهائية على مشروع Windows الأصلي. مجلد `server/` داخل مشروع Android محفوظ لاختبارات عقد API والتوافق فقط، وهو مستبعد من حزمة APK بواسطة إعدادات `pyproject.toml`.

## نتيجة الإصلاح

- إصلاح الاستعادة من قاعدة قديمة بصيغ DB/SQLite/ZIP وZIP المتداخل.
- ترحيل آمن على نسخة مؤقتة إلى schema version 23 قبل استبدال قاعدة التطبيق.
- نسخة أمان تلقائية قبل الاستعادة وrollback ذري لقاعدة البيانات و`config.json` عند الفشل.
- الحفاظ على القيود والمبالغ والتواريخ والملاحظات والعملات والمستخدمين القدامى.
- دعم كلمات المرور القديمة PBKDF2-SHA256/100,000 وترقيتها تلقائيًا إلى PBKDF2-SHA256/600,000 بعد الدخول الصحيح.
- إزالة رموز جلسات الشبكة من SQLite ومن النسخ الاحتياطية.
- فرض HTTPS افتراضيًا وقصر HTTP الاختياري على الشبكات الخاصة مع موافقة صريحة.
- إزالة صلاحيات التخزين الشامل والوسائط غير اللازمة من Android.
- إصلاح حوار استيراد آخر نسخة داخلية.
- حماية تصدير CSV بقائمة جداول مسموحة.
- تنظيف وتنسيق الكود وإزالة جميع مخالفات Ruff.

## الاختبارات

- جميع اختبارات المشروع: 67/67 ناجح، 0 فشل، 0 timeout.
- بوابة الإصدار `tools/quality_gate.py`: ناجحة.
- `tools/apk_release_preflight.py`: ناجح.
- Ruff: All checks passed.
- Bandit: 0 High، 0 Medium؛ التحذيرات المتبقية Low مرتبطة بمسارات fallback والاستثناءات الدفاعية.
- `python -m compileall -q .`: ناجح.
- `pip check`: لا توجد تبعيات مكسورة.
- بناء Wheel وsdist: ناجح.

## بناء APK

تعذر إنتاج APK في بيئة الفحص لأن Flet حاول تنزيل Flutter 3.29.2 وفشل DNS قبل بدء تجميع Android. هذا قيد في الشبكة الخارجية للبيئة، وليس خطأ تجميع من كود المشروع. يجب بناء APK على جهاز متصل بالإنترنت ثم اختباره على أجهزة Android فعلية.

## قاعدة البيانات القديمة الخاصة بالمستخدم

تم اختبار ترحيل قاعدة قديمة مصطنعة مطابقة للبنية السابقة، بما في ذلك بيانات متعددة العملات، مستخدم قديم، تجزئة كلمة مرور قديمة، token قديم، و`config.json` تالف. لم يتم فحص ملف قاعدة المستخدم الفعلي لأنه لم يُرفع في المحادثة؛ لذلك يجب الاحتفاظ بالأصل حتى التحقق اليدوي بعد الاستيراد.

## تنبيه سرية النسخ الاحتياطية

النسخة الاحتياطية منزوعة رمز جلسة الشبكة لكنها ليست مشفرة تشفيرًا تشغيليًا. تظل محتوية على البيانات المحاسبية وحسابات المستخدمين والأملاح وتجزئات كلمات المرور. يجب تخزينها في مكان موثوق وعدم مشاركتها دون ضرورة.
