# المرحلة 106 — جدول قيود الشركة والحذف المباشر للعمليات

## نطاق التعديل

تحولت قيود شاشة تفاصيل الشركة من بطاقات متتابعة إلى جدول محاسبي يحتوي على:

- التاريخ.
- البيان.
- مبلغ «لنا».
- مبلغ «له».
- الرصيد الجاري بعد القيد.
- مصدر القيد.
- قائمة الإجراءات.

الجدول قابل للتمرير الأفقي على شاشة الهاتف، ويظل الرأس متكيفًا مع العرض الضيق.

## احتساب الرصيد الجاري

يُحسب الرصيد الجاري من جميع قيود الشركة بترتيب ثابت:

1. تاريخ القيد.
2. وقت الإنشاء أو التحديث.
3. رقم القيد.

لا يتغير الرصيد الجاري بسبب البحث أو تصفية الصفوف الظاهرة؛ التصفية تخفي الصف فقط ولا تعيد بناء التاريخ المالي من مجموعة ناقصة.

## سياسة الحذف المطلوبة

### القيد العادي

يبقى قابلًا للحذف المباشر من صف الجدول، ولا يُنشأ له قيد عكسي.

### الخدمة المباشرة

زر الحذف يحذف العملية الأصلية كاملة، بما يشمل:

- قيد العميل.
- قيد المورد، إن وجد.
- قيود العكس التاريخية، إن كانت العملية قد عُكست في إصدار سابق.
- تذكيرات الدفع المرتبطة.
- سجل `direct_services` الأصلي.

### ملف الخدمة وباقي الخدمات المركبة

زر الحذف يحذف ملف الخدمة كاملًا، بما يشمل:

- قيد العميل.
- قيود جميع الموردين والمكونات.
- قيود العكس التاريخية.
- مكونات ملف الخدمة.
- التذكيرات المرتبطة.
- سجل `service_cases` الأصلي.

### السداد بالنيابة

يحذف طرفي العملية والسجل الأصلي وأي قيود عكس تاريخية مرتبطة.

## ضمانات سلامة البيانات

- لا يسمح بحذف صف مولّد من خدمة منفردًا؛ يجب حذف العملية المصدر كاملة حتى لا يبقى طرف دون الطرف المقابل.
- جميع عمليات الحذف المرتبطة تنفذ داخل معاملة SQLite واحدة `BEGIN IMMEDIATE`.
- في حال فشل أي خطوة، يجري `ROLLBACK` ولا يحدث حذف جزئي.
- سبب الحذف إلزامي للعمليات المرتبطة.
- يحتفظ `audit_log` بمرجع العملية، والسبب، وعدد القيود المحذوفة، دون الاحتفاظ بقيود مالية عكسية.
- مستخدم `viewer` لا يحصل على إجراءات التعديل أو الحذف.

## وضع الشبكة

أضيفت طلبات DELETE المتناظرة إلى العميل والخادم:

- `DELETE /api/direct_services/<reference>`
- `DELETE /api/service_cases/<reference>`
- `DELETE /api/third_party_payments/<reference>`

هذه المسارات محمية بصلاحيات الكتابة، وتتطلب سبب الحذف في جسم الطلب.

## الاختبارات

نجحت الاختبارات التالية:

- ترجمة جميع ملفات Python بواسطة `compileall`.
- اختبار جدول قيود الشركة وسياسة الحذف الجديدة.
- حذف قيد عادي دون إنشاء عكس.
- حذف خدمة مباشرة بطرفيها.
- حذف خدمة مباشرة كانت معكوسة تاريخيًا، مع إزالة الأصل والعكس معًا.
- حذف ملف خدمة متعدد الموردين والمكونات.
- حذف عملية سداد بالنيابة بطرفيها.
- اختبارات CRUD والعملات ومسارات الخدمات وعقود REST ذات الصلة.

بدأت بوابة الجودة الكاملة ونجحت الاختبارات السابقة لاختبار نظام التصميم. توقفت عند `phase100_design_system_smoke_test.py` لأن حزمة `flet` غير مثبتة في بيئة الفحص، وليس بسبب فشل تحقق متعلق بالتعديل. لم يُبنَ APK ولم تُشغّل واجهة Flet أو خادم Flask فعليًا في هذه البيئة.

## تنبيه تشغيلي

الحذف هنا حذف نهائي مقصود وليس إلغاءً محاسبيًا. يؤدي حذف خدمة مرتبطة إلى تغيير أرصدة جميع الشركات المشاركة فورًا. لذلك تعرض الواجهة تحذيرًا صريحًا، وتطلب سببًا قبل التنفيذ.
